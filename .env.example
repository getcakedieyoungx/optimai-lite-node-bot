# API URL (varsayılan: https://api.optimai.network)
API_URL=https://api.optimai.network

# Refresh Token (accounts.json kullanılmıyorsa gerekli)
REFRESH_TOKEN=your_refresh_token_here

# Ping zamanlaması (varsayılan: her 5 dakikada bir)
# PING_CRON=*/5 * * * *

# Daily claim zamanlaması (varsayılan: rastgele bir saat)
# DAILY_CLAIM_CRON=0 12 * * *

# Weekly claim zamanlaması (varsayılan: rastgele bir gün ve saat)
# WEEKLY_CLAIM_CRON=0 12 * * 0